<br>

<div class="row">
  <form action="/men/new" method="GET" class="d-flex align-items-center">
    <!-- Search Input -->
    <div class="col-md-7">
      <input  type="text" style="width: 95%;" placeholder="Search for..." name="keyword" value="<%= searchKeyword %>">
    </div>

    <!-- Select Color Dropdown -->
    <div class="col-md-2">
      <label for="color" class="mr-2">Select color:</label>
      <select name="color" id="color" class="form-control">
        <option value="">All Colors</option>
        <% colors.forEach(color => { %>
          <% if (color.name === selectedColor) { %>
            <option selected value="<%= color.name %>"><%= color.name %></option>
          <% } else { %>
            <option value="<%= color.name %>"><%= color.name %></option>
          <% } %>
        <% }) %>
      </select>
    </div>

    

    <!-- Apply Filter Button -->
    <div class="col-md-1">
      <button class="buttn" type="submit"><i class="bi bi-search"></i></button>
    </div>
  </form>
</div>


<br>
<%- include('productGrid', { products: products, large: true }) %>
<br>

<nav aria-label="Page navigation">
  <ul class="pagination">
    <% if (currentPage > 1) { %>
      <a class="bttn  <%= currentPage === 1 ? 'disabled' : '' %>" href="?page=1&color=<%= selectedColor %>" tabindex="-1">First</a>
      <a class="bttn " href="?page=<%= currentPage - 1 %>&color=<%= selectedColor %>">Previous</a>
    <% } %>
    
    <% for (let i = 1; i <= totalPages; i++) { %>
      <% if (i === parseInt(currentPage)) { %>
        <button class="bttn  active" href="?page=<%= i %>&color=<%= selectedColor %>"><%= i %></button>
      <% } else { %>
        <a class="bttn " href="?page=<%= i %>&color=<%= selectedColor %>"><%= i %></a>
      <% } %>
    <% } %>
    
    <% if (currentPage < totalPages) { %>
      <a class="bttn " href="?page=<%= currentPage + 1 %>&color=<%= selectedColor %>">Next</a>
      <a class="bttn  <%= currentPage === totalPages ? 'disabled' : '' %>" href="?page=<%= totalPages %>&color=<%= selectedColor %>">Last</a>
    <% } %>
  </ul>
</nav>



<div class="bottom-banner" style="width: 100%;">
  <div class="row">
    <div class="col-md-6">
      <div class="image-container">
        <img src="/images/bott2.jpg" alt="Image description" class="bottom-banner-cover">
        <div class="centered-content">
          <p class="centered-text">Coat</p>
        </div>
        <a class="buttn bttn-secondary centered-button">Shop now</a>
      </div>
    </div>
    <div class="col-md-6">
      <div class="image-container">
        <img src="/images/bott1.jpg" alt="Image description" class="bottom-banner-cover">
        <div class="centered-content">
          <p class="centered-text">Jackets</p>
        </div>
        <a class="buttn bttn-secondary centered-button">Shop now</a>
      </div>
    </div>
  </div>
</div>




<!-- Your custom styles here -->
<style>



  .pagination .bttn.active {
    background-color: rgb(194, 214, 205) !important;
    border-color: rgb(0, 0, 0) !important;
    color: rgb(0, 0, 0) !important;
  }

  .pagination {
    display: flex;
    align-items: center;
    justify-content: center;
    margin-top: 2rem;
  }
  .bottom-banner-cover {
  object-fit: cover;
  width: 100%;
  height: 17em;
}

.image-container {
  position: relative;
  width: 100%;
  height: 100%;
}

.centered-content {
  position: absolute;
  top: 75%;
  left: 50%;
  transform: translate(-50%, -50%);
  text-align: center;
}

.centered-button {
  margin-bottom: 1rem;
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
}

.centered-text {
  font-weight: 500;
  font-size: 2rem; /* Adjust the font size as needed */
  color: white; /* Adjust the text color as needed */
}

</style>
